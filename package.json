{
  "name": "critz-tycoon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A tiny world. A living ecosystem. A new beginning.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs --dist",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/main.js && node --check src/state.js && node --check src/world.js && node --check src/art.js"
  },
  "engines": {
    "node": ">=20"
  }
}
